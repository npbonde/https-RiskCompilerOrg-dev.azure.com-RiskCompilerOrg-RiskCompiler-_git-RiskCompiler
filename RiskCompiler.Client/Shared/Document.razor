@inject IJSRuntime JSRuntime

@code {
    [Parameter]
    public string? Title { get; set; }

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
            await JSRuntime.InvokeVoidAsync("RiskCompiler.setDocumentTitle", Title);
    }
}